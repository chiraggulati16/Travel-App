import {StyleSheet} from 'react-native';
import { moderateVerticalScale } from '../../../../utils/scaling';


export const styles = StyleSheet.create({
  banner: {
    width: "100%",
    height: moderateVerticalScale(490)
  }
});
